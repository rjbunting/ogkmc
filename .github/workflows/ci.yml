name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[cli,test,dev]"
      - run: python -m compileall -q autokmc
      - run: ruff check autokmc tests
      - run: mypy --follow-imports=skip autokmc/io/config.py autokmc/io/checkpoint.py autokmc/analysis/products.py
      - run: pytest --cov=autokmc --cov-report=term-missing --cov-report=xml --cov-fail-under=50
